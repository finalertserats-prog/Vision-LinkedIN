"""Synthesise package — the generate -> critique -> verify chain (§13.1).

Empty in this scaffold; the RAFT prompt contracts, JSON schemas and grounding
gate land in Phase 1. Present so ``import vision.synthesise`` resolves.
"""
