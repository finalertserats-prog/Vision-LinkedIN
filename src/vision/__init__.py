"""Project VISION — daily AI-assisted LinkedIn insight engine.

Top-level package. Exposes the package version so tooling and the ``/healthz``
endpoint can report the running build.
"""

# Single source of truth for the runtime version; mirrors pyproject.toml.
__version__ = "0.1.0"
