"""Brahmastra synthesis-engine adapter package.

Holds the ``BrahmastraClient`` / ``BrahmastraImageClient`` adapters (BRD §13.0)
that wrap the local council CLI scripts (gemini_call.sh, codex_call.sh, etc.).
Intentionally empty in this scaffold — the client is implemented by a later
agent; this file exists so ``import vision.brahmastra`` resolves.
"""
